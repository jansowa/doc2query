# Rejestr zadań

Ten plik jest centralnym spisem treści i źródłem prawdy o stanie realizacji
programu. Szczegółowe wymagania oraz kryteria akceptacji pozostają w plikach
poszczególnych zadań.

## Zasady aktualizacji

Każda sesja realizująca zadanie musi w tym samym commicie:

1. zaktualizować jego status w poniższej tabeli;
2. zaktualizować sekcję `Status` w pliku zadania;
3. opisać w kolumnie „Stan i następny krok” faktycznie wykonany zakres oraz
   niewykonane pomiary, kosztowne runy lub bramki;
4. nie oznaczać zadania jako ukończonego na podstawie samej implementacji, jeśli
   jego kryteria wymagają wyników eksperymentalnych.

Dozwolone statusy:

- `TODO` — prace nie zostały rozpoczęte;
- `IN PROGRESS` — trwa implementacja lub walidacja;
- `IMPLEMENTED` — kod i tanie testy są gotowe, ale pozostały jawnie wskazane
  runy, pomiary albo bramki;
- `DONE` — wszystkie kryteria akceptacji zostały spełnione;
- `BLOCKED` — dalsza praca zależy od wskazanej bramki lub decyzji;
- `OPTIONAL` — eksperyment nie należy do domyślnej ścieżki.

Statusy muszą opisywać stan potwierdzony artefaktami. Nie wolno wpisywać
wyników treningu, benchmarku ani eksperymentu, którego faktycznie nie
uruchomiono.

## Spis i stan realizacji

| Task | Zakres | Status | Stan i następny krok |
|---|---|---|---|
| [00](00_repository_bootstrap.md) | Bootstrap repozytorium i odtwarzalność | `DONE` | Szkielet projektu, środowisko, CLI, testy i rejestrowanie są gotowe. |
| [01](01_data_contract_audit_and_splits.md) | Kontrakt danych, audyt, deduplikacja i splity | `IMPLEMENTED` | Pełny `msmarco_pl` przetworzono do zamrożonych splitów v1 i par doc2query bez leakage pozytywów. Dla rekordów z <10 negatywami przyjęto corpus retrieval oraz oznaczone, deterministyczne backfillowanie tylko w diagnostycznej puli. Pozostał raport tokenowych percentyli/HTML. |
| [02](02_reranker_and_reward_proxies.md) | Zamrożone rerankery i proxy nagrody | `IMPLEMENTED` | Integracja, reward proxies i testy są gotowe; primary zmierzył pełny frozen dev, a query-macro próg Youdena `possible_false_negative` jest przypięty bez użycia testu. Bramka HN domierzyła primary/shadow na 775 wspólnych dev query z 10 negatywami. **Pełny benchmark obu sędziów na całym frozen dev uruchomiony 2026-08-23** (`scripts/run_task02_full_dev_judge_benchmark.py`, `.venv-gpu`): 16 272 rekordy pocięte na 34 wznawialne shardy po 500, shard z gotowym `benchmark.json` jest pomijany, dziennik `journal.jsonl` notuje każdy ukończony, zmierzony koszt 177 s/shard (oba sędziowie, ~100 min GPU łącznie, peak 4,5 GB). Shardy są **grupowane w pasma liczby negatywów** (`neg10`, `neg07_09`, `neg03_06`), bo frozen dev ma rekordy z 3-10 negatywami, a ranking na trzech negatywach nie jest porównywalny z rankingiem na dziesięciu — populacje nie są mieszane w jednej liczbie. Run **nie zmienia** żadnego progu (w tym przypiętego Youdena), wag sędziów ani rubryki i **nie otwiera testów finalnych**: wejściem jest wyłącznie `dev.parquet`, a runner odrzuca ścieżkę testową. **Wynik zmierzony tego samego dnia**: oba sędziowie mają teraz **16 258 z 16 272 rekordów** frozen dev, czyli **21×** więcej niż 775 query bramki HN, w ~100 min GPU. Pomiar idzie w pasmach liczby negatywów, których **nie wolno sumować** (Recall@k i nDCG@10 zależą od rozmiaru puli; każdy raport nosi `bands_must_not_be_pooled: true`, a minimum jest przekazywane jawnie flagą `--min-hard-negatives` o domyślnej wartości 10, czyli poprzedni kontrakt bez zmian): `neg10` 6 598 query, `neg07_09` 8 959, `neg04_06` 701. Wyłączone jest **14 rekordów (0,086%)** z pulą poniżej 5 kandydatów, gdzie Recall@5 jest matematycznie nieokreślony — to jedyna nieoglądana część dev. Query-macro: w `neg10` primary bije shadow na każdej mierze (R@1 **0,9457** vs 0,9218, MRR 0,9656 vs 0,9463, nDCG@10 0,9736 vs 0,9584, ujemny margines 5,43% vs 7,82%), w `neg07_09` przewaga topnieje (R@1 0,9327 vs 0,9228), a w `neg04_06` **odwraca się** (0,9489 vs **0,9584**), spójnie na wszystkich czterech miarach, przy n=701, więc jako przesłanka, nie rozstrzygnięcie. Wniosek jest wąski: domyślny wybór primary jest uzasadniony tam, gdzie faktycznie działa reward proxy (pełne pule 10 negatywów), a nie jako teza o globalnej wyższości; mniejsze pule są przy tym z natury łatwiejsze i pochodzą z uboższego kopania negatywów, więc różnią się od `neg10` nie tylko rozmiarem puli. Rozbieżność sędziów: rank 9,56% / 9,91% / 6,60% w kolejnych pasmach, Pearson marginesów 0,6547 / 0,6239 / 0,5670 — wartość dla `neg10` na 8 096 grupach praktycznie powtarza 9,81% z bramki HN na 775 query, więc tamta liczba **nie była artefaktem małej próbki**. **Dwie luki zostają jawnie otwarte**: wymagane slice'y są nieosiągalne z powodu zdegenerowanych metadanych frozen dev (`domain`/`query_type`/`difficulty` = `unknown`, `source_en_difficulty` = `easy`), co jest luką kontraktu danych Task 01, oraz część testowa benchmarku pozostaje zamknięta. Żaden próg nie został ustalony ani przeliczony, przypięty Youden bez zmian. Raport: `reports/measurements/task02_full_dev_judge_benchmark_2026-08-23.md`. |
| [03](03_sft_qlora_baselines.md) | Baseline'y SFT/QLoRA | `IMPLEMENTED` | S07 jest kompletną, nieporównywalną budżetowo diagnostyką bez promocji. P-06 mass rescoring jest `SUPERSEDED`. Właściciel świadomie anulował ręczne P06-T i zaakceptował resztkowe ryzyko tłumaczeń na podstawie wcześniejszego udanego treningu embeddera; frozen train i próg `>=23.50` pozostają bez zmian. Nie kończyć lokalnego scoringu ani trenować drop/weighted. |
| [04](04_evaluation_harness.md) | Harness ewaluacyjny | `IMPLEMENTED` | P-01–P-05 i pełny S07 Harness/probe są gotowe. Pełna dev-only bramka HN0–HN3 ukończyła 775 wspólnych legalnych query i utrzymała HN0+filter/drop z powodu braku zgodnej primary/shadow podstawy do promocji nowego minera; nie otwarto testów finalnych. Artefakt S07 pozostaje `comparison_eligible=false`; pozostały porównywalne probe i testy finalistów. Rozszerzenie 2026-08-13: do zakresu dodano M-03 (guardrail zbieżności probe, 5 seedów na tanich korpusach). M-03 jest od 2026-08-16 **zamrożony prospektywnie**: ADR `reports/decisions/task04_m03_probe_convergence_guardrail_v1.md` przypina sygnał zbieżności **retrievalowy** (`corpus_recall_at_100`, `loss_based_guardrail_permitted=false`, bo zmierzone `r = −0,199` wyklucza stratę), podłogę niezależną od ramienia (`max(4× poziom losowy, ½ mediany wspólnej obu ramion)`), sparowane odrzucanie całych par seedów, obowiązkowy wynik bez filtra oraz decyzję wymagającą ≥5 zbieżnych par seedów, dolnej granicy 95% CI bootstrapu po seedach ≥ `+0.01` i dokładnego jednostronnego testu znakowego `p ≤ 0.05` (minimum 5 seedów nie jest okrągłe: przy 4 parach najmniejsze osiągalne `p = 0,0625 > 0,05`, więc reguła byłaby nierozstrzygalna). Progi skalibrowano na 22 zakończonych runach i zamrożono; guardrail oznaczył 4/22 runy jako niezbieżne, dokładnie cztery najniższe po sygnale, przy 2,3× separacji do pierwszego runu zdrowego. Diagnostyka 5 seedów confirmu TriviaQA: po odrzuceniu pary S43 (W06 nie zbiegł) zostają 4 zbieżne pary, czyli status `insufficient_converged_seeds`; średnia bez filtra `+0,0319`, po filtrze `+0,0143` (CI `[+0,0025,+0,0238]`). Zamknięty confirm **nie** jest unieważniony ani powtarzany, nic nie zostało wypromowane (`promotion_authorized=false`). Raport: `reports/measurements/task04_m03_probe_convergence_calibration_2026-08-16.md`. Metrologia przyrządu 2026-08-17 (pięć replikatów tego samego ramienia W06, różny tylko seed): **zapadnięcia są prawidłowością** — 1/5 w serii, łącznie z M-03 **5/27 = 18,5%**, więc porównanie dwóch ramion na trzech seedach ma ~71% szans, że zawiera zapadnięty run. Wariancja wewnątrz ramienia jest natomiast znośna (sd `corpus_ndcg_at_10` 0,0046, CV 8,8%, półszerokość CI 0,0052 przy n=3), więc podejrzenie braku rozdzielczości na próg +0,01 było przedwczesne. Reguła `last_loss >= first_loss` wykrywa 2/5 zapadnięć przy zerowych fałszywych alarmach na 22 runach i łapie W06-S43. Wniosek operacyjny: detekcja w trakcie runu i auto-reseed zamiast dokładania seedów — wymaga prospektywnego ADR, bo dotyka `embedder_probe.py`. Raport: `reports/measurements/task04_probe_within_arm_variance_2026-08-17.md`. Detekcja w trakcie runu 2026-08-22: prospektywny ADR `reports/decisions/task04_m03_in_run_collapse_detection_v1.md` zamrożono **przed pierwszym nowym runem** (utrwalanie pełnej krzywej straty i pośredniej ewaluacji retrievalowej co 256 kroków na zbiorze zbudowanym wyłącznie z par treningowych runu, podłoga = ta sama stała 4× poziomu losowego co M-03, kierunek straty tylko jako tani fail-fast przy `loss_based_guardrail_permitted=false`, przerwanie dopiero po dwóch kolejnych trafieniach, reseed max 3 próby z seedem `requested + 1000 × numer próby` i obowiązkowym zapisem liczby prób, wyczerpanie prób kończy run błędem bez artefaktu `measured`). Implementacja jest **domyślnie wyłączona** (`src/doc2query/evaluation/probe_in_run_collapse.py`, flaga `--collapse-detection-config`, 16 testów CPU). **Wszystkie pięć prerejestrowanych kryteriów przeszło** na pięciu runach walidacyjnych tanim korpusem: run kontrolny z wyłączoną flagą odtworzył zamrożony S47 **bit w bit**, więc niedeterminizm GPU wyszedł zerowy i identyczność trajektorii rozstrzygnęła się przy dopuszczalnej różnicy 0 (S48 i S51 z detekcją też odtworzyły co do cyfry zamrożone wartości). Seed 50 dał **dwa zapadnięcia z rzędu** przerwane na kroku 512 (recall pośredni 0,422 i 0,141 przy podłodze 0,521), przyjęto dopiero seed 2050; separacja jest czysta (zapadnięte 0,141–0,422, zdrowe 0,844–0,977, zero przypadków pośrednich na 15 kontrolach), zapadnięty przebieg kosztuje 99 s zamiast ~1500 s, a koszt własny mechanizmu to ~10 s na run. Kierunek straty **przeoczył** zapadnięcie seeda 1050 (strata malejąca przy recallu 0,141), co potwierdza jego rolę fail-fastu, nie guardraila. Zero fałszywych alarmów na trzech zdrowych seedach, ale to 0/3 runów (górna granica 95% CI ~0,63), więc swoistość **nie jest** wykazana. Cena reseedu jest już widoczna: przyjęty seed 2050 dał `corpus_ndcg_at_10` 0,0832 powyżej całego zakresu serii zamrożonej (0,0475–0,0582), a sd serii wzrosło do 0,0152 wobec 0,0046 — reseed warunkuje rozkład seedów, więc średnich tej serii nie wolno czytać jako oszacowania ramienia W06, a detekcję trzeba włączać symetrycznie w obu ramionach porównania. Podłoga wyszła 0,521 zamiast zakładanych 0,195, bo pula pośrednia ma 768, a nie 2048 unikalnych pasaży (K=4); zamrożona reguła zadziałała dokładnie jak zapisano, a trudniejsze kryterium i tak przeszło. Zamknięte pomiary (M-03, confirm TriviaQA, sweep budżetu, seria wariancji) **nie są przeliczane ani unieważniane**, ich artefakty pozostają nietknięte, mieszanie runów o różnym ustawieniu detekcji w jednym porównaniu jest zabronione, a guardrail M-03 pozostaje jedynym organem orzekającym o zbieżności ukończonego runu. Wznawialność potwierdzona w praktyce: wyłączenie maszyny w trakcie runu nie skasowało pracy (dokończenie w 270 s z cache'u, dziennik prób przetrwał). Raport: `reports/measurements/task04_m03_in_run_collapse_detection_2026-08-21.md`. Charakterystyka detektora 2026-08-23: amendment `reports/decisions/task04_m03_in_run_collapse_shadow_mode_amendment_2026-08-22.md` dodał **tryb obserwacyjny** (kontrole liczone, run nigdy nieprzerywany), bo inaczej nie istnieje prawda odniesienia dla fałszywego alarmu. Na 20 runach jednego ramienia (seedy 52–71): **zero fałszywych alarmów na 19 zdrowych runach** (95% CI [0; 0,176]), jedno zapadnięcie (seed 64, `recall@100` i `ndcg@10` dokładnie 0,000000) wykryte na kroku 512. **Wymóg dwóch kolejnych trafień okazał się konieczny:** dwa zdrowe runy miały pojedyncze trafienie na kroku 256 i wróciły do normy, więc wariant jednotrafieniowy dałby 2/19 = 10,5% fałszywych alarmów. Kierunek straty nie trafił ani razu (łącznie 1 z 3 zapadnięć w obu seriach). Odsetek zapadnięć 1/20 = 5,0% (CI [0,1%; 24,9%]) nie zastępuje szacunku 18,5% z mieszanki konfiguracji. **Rozdzielczość przyrządu skorygowana w dół:** sd `corpus_ndcg_at_10` na 19 zbieżnych runach to **0,0142**, nie 0,0046 z n=4, więc półszerokość 95% CI wynosi 0,0160 przy n=3; dla statystyki decyzyjnej liczy się sd par (0,0126), co daje 0,0110 przy 5 parach i 0,0078 przy 10 — minimum 5 par z M-03 leży na granicy progu `+0,01`. Koszt kontroli ~10 s na run. Raport: `reports/measurements/task04_m03_collapse_detector_specificity_2026-08-23.md`. `task07_training_authorized=false`, `final_tests_used=[]`. |
| [05](05_controlled_diversity_and_multiquery.md) | Kontrolowany styl, focus i multi-query | `IMPLEMENTED` | TriviaQA 3-seed confirm przeszedł (`nDCG@10 +0.0478666`, 97.5% CI `[+0.0450118,+0.0508263]`); analiza 42+44 utrzymuje efekt mimo niestabilnego W06 S43. Właściciel zaakceptował W06+D01+selector jako procedurę danych Task 06 i D01 controlled jako przyszły start Task 07. Handoff preflight jest verified, ale pełne 4.5B, Task 09 i final test pozostają nieautoryzowane. |
| [06](06_candidate_scoring_and_preference_data.md) | Scoring kandydatów i dane preferencyjne | `IN PROGRESS` | Pilot 512×8 ukończony: 4096/4096 scoringów i 2048 safe-selected (1164 W06 + 884 D01), selector zmienił 482/512 grup. Naprawiono wyłącznie błędne etykiety `smoke` w provenance i odbudowano selekcję; teksty/score’y bez zmian. Pilot nie wystarcza do DPO, bo sloty D01 mają różne prompty. Zamrożono więc same-prompt expansion: 500 promptów D01 × 8 odpowiedzi, potem scoring, tentative pairs i dual-LLM Groq. Run expansion jest ukończony (4000/4000 generacji i scoringów, bez resume, peak VRAM 3.43 GB), a jego grupowe metryki pokazały kolaps różnorodności przy identycznym promptcie. Progi bramki różnorodności same-prompt zamrożono prospektywnym ADR i zastosowano bramkę na CPU: 362/500 grup `eligible` (72.4%), 138 odrzuconych. Kohorta daje najwyżej 362 pary, czyli mniej niż 500 wymaganych do rozwojowej bramki dual-LLM. Właściciel delegował wybór ścieżki naprawy; ADR v2 zamraża nową, rozłączną klastrowo kohortę 500 pasaży z szerszym decodingiem. Etap CPU jest wykonany (500/500 unikalnych klastrów, zero nakładania z 544 klastrami smoke/pilot i 49352 klastrami 50k SFT), kohorta v2 jest ukończona (4000/4000 generacji i scoringów; jeden run przerwał się na niepoprawnym completion w slocie 1.2, po amendmencie resamplującym wznowił się bez utraty pracy), a bramka dała 466/500 grup `eligible` (93.2%) wobec 362/500 w v1 — razem 828 grup, powyżej progu 500 par. Na okno bezobsługowe 2-3 dób zamrożono rozłączne kohorty v3/v4/v5 po 3000 pasaży i uruchomiono nadzorowaną kolejkę (`scripts/run_unattended_queue.sh`, watchdog w cronie co 20 min i po restarcie) na ~53 h: M-03 (seedy 45/46 confirmu TriviaQA, wyłącznie treningi), dziewięć rozłącznych kohort v3-v11 po 3000 pasaży oraz sweep budżetu probe 1024/2048 jako diagnostyka M-01/M-03. Po wyczerpaniu kolejki osobny strażnik (`scripts/poweroff_when_queue_drained.sh`, cron 5/25/45) wyłącza maszynę po godzinie stabilnego stanu wyczerpania. Kolejka produkuje wyłącznie artefakty; agregacje i decyzje czekają na właściciela. Par nie zbudowano, Groq nie uruchomiono, Task 07 nadal zamknięty, `final_tests_used=[]`. Rozszerzenie 2026-08-13: dopuszczono prospektywną ablację teachera (lokalny Qwen3.6-27B Q4) i lokalnego sędziego qwen3.6-27b za osobnym ADR. Okno tokenowe 2026-08-14/16 (komenda właściciela: generować dane bezpośrednio tokenami modelu asystującego): zamrożono prospektywnie dwa ADR-y przed generacją. Korpus walidacyjny nagrody (180 pasaży × 8 klas = 1440 zapytań o etykietach z konstrukcji) jest **ukończony i zmierzony na CPU**: PASS dla P1/P3-wrong-focus/P4-dobre/P5/P6/P7 (bramka różnorodności o niezmienionych progach przepuściła 180/180 grup), FAIL dla P2 (`entity_preservation` to detektor halucynowanych encji, nie sygnał specyficzności — remis 1.0 w 180/180), P3-good-specific (0.622; 46 focusów nierozstrzygniętych) i P4-wrong-form (0.75; `_PREFIX` wymaga dwukropka, więc „Oto …” przechodzi). Progów nie kalibrowano, `format.py` i splittera zdań nie zmieniono. Ablacja teachera na modelu API (600 pasaży kohorty v3 × 4 kontrolki × 4 kandydatury, osobne provenance, wagi nieprzypięte) jest **ukończona na etapie generacji**: 24/24 shardów, 9600/9600 rekordów, walidacja bez błędów. Sygnał uboczny: `intent_fit=strained` w 32.6% rekordów, z czego `procedure`@end aż 60.6% — zamrożona kontrolka nie ma pokrycia w większości pasaży `msmarco_pl`; `passage_quality_note` dla 266/600 pasaży (44.3%). Amendment 2026-08-16 (komenda właściciela po zwolnieniu GPU, warunek: wznawialność) autoryzuje **wyłącznie scoring** obu kohort tokenowych zamrożonym kontraktem oraz pomiar prerejestrowanych kryteriów — bez zmiany predykcji i progów. Wejścia zmaterializowano idempotentnie w zamrożonym schemacie; kontrakt same-prompt potwierdzony: 600/600 pasaży ma `prompt_sha256` identyczny ze studentem v3. Scoring korpusu nagrody ukończony (1440/1440, 175 s) i **P8 zmierzone**: P8a PASS (95,6% wobec progu 0,85; kontrola shadow 96,7%), P8b zapisana kierunkowo bez progu, więc bez werdyktu (124/180 słabszych, 54 remisy, średnie 0,750 vs 0,072). Wznawianie potwierdzone w praktyce (`resume 944/1440`). Kluczowy wynik dla polityki par: sygnały sędziowskie najmocniej nagradzają kopiowanie (`copy_verbatim` ma najwyższy `pool_margin` w 52,8% grup), więc odziedziczony z Task 05 guard `reject_chosen_on_copy_risk` jest nośny — łapie 180/180 kopii, kosztem 29,4% poprawnych krótkich zapytań `keyword_query`; nic nie zmieniono. Scoring kohorty teachera ukończony (9600/9600) i porównanie policzone na 600/600 par (pasaż, kontrolka) o identycznym `prompt_sha256`: **ablacja wypadła negatywnie** — teacher API nie bije lokalnego D01 4.5B na zamrożonym sygnale budującym (primary 34,7% literalnie, 41,5–41,7% równobudżetowo post-hoc; shadow 54,7–56,7%, ale kierunek primary/shadow rozchodzi się w 22,0% grup; round-trip 76–85% remisów). Teacher jest bardziej równy (średni margines 4,19 vs 2,88), student wygrywa maksimum — efekt best-of-N. Znalezisko P8 potwierdzone na danych produkcyjnych: argmax po primary łamie guard `copy_risk` w 24,8% (teacher) i 28,8% (student) przypadków. Wynik zdejmuje presję wprowadzania teachera o nieprzypiętych wagach; probe-embeddera nie uruchamiano. Budowa par i Groq pozostają nieautoryzowane; kolejka bezobsługowa zakończyła się 2026-08-16 wynikiem 25/25 zadań bez awarii w 49,18 h GPU: kohorty v3-v11 dały 25164/27000 grup `eligible` (93,2%), co z v1 i v2 daje 25992 grupy do budowy par (powyżej progu 1000), a sweep budżetu probe pokazał, że wariancja seedowa (0,0011-0,0826) bywa większa od mierzonych efektów i że strata treningowa nie jest guardrailem zbieżności (`r = -0,199`). GPU jest wolne. Autoryzacja właściciela 2026-08-16 domknęła politykę par: prospektywny ADR `reports/decisions/task06_tentative_pair_policy_v1.md` zamroził ją **przed odczytem jakiejkolwiek pary** (primary `pool_margin` jedynym sygnałem budującym, shadow wyłącznie veto, corpus round-trip niezależnym filtrem `chosen`@20/`rejected`@100, `top_vs_near_miss`, max 1 para na prompt, minimalny margines primary 1.0 jako jedna jednostka log-odds, `entity_preservation` wykluczone, `focus_accuracy` tylko słaby filtr z nieskarną abstencją, zmierzona ślepa plamka `format_valid` domknięta osobnym guardem wtrącenia bez zmiany `format.py`). Fail-closed builder (`src/doc2query/preferences/pair_policy.py`, `scripts/build_task06_tentative_pairs.py`, 18 testów CPU) zbudował pary **wyłącznie z v1+v2**: 202 pary z v1 (55,8% grup `eligible`) i 245 z v2 (52,6%), łącznie **447** — czyli **poniżej progu 500** rozwojowej bramki dual-LLM, co uruchamia prerejestrowaną ścieżkę niedoboru (audyt na wszystkich parach, żadnego poluzowania progów, rozszerzenie na v3 wymaga amendmentu właściciela). Veto shadow zadziałało 85 razy (10,3% grup `eligible`). Kohorty v3–v11 dostaną pary tą samą polityką dopiero po pozytywnym audycie. Raport: `reports/measurements/task06_tentative_pairs_v1_v2_2026-08-16.md`. Ślepy eksport audytowy jest **zamrożony**: `artifacts/task06/preference_audit_v1/` zawiera 447 ślepych par (dokładnie pięć dozwolonych pól, zero wycieku `chosen`/`rejected`/marginesów), rozdzielony klucz odślepiający, pełne rekordy próbki oraz zobowiązanie do orientacji A/B podjęte przed jakąkolwiek oceną (`sha256(salt‖pair_id‖orientacja)`, sól opublikowana w manifeście, 447/447 zobowiązań zweryfikowanych). Orientacja jest kontrbalansowana 224/223. Niedobór 53 par rozstrzygnięto 2026-08-17 decyzją właściciela: **dopuszczono kohortę v3**, nie obniżono bramki. Amendment `reports/decisions/task06_tentative_pair_policy_v3_topup_amendment_2026-08-17.md` tworzy politykę v1.1 różniącą się od v1 **wyłącznie** listą `authorized_cohorts`; pary przebudowano do `tentative_pairs_v1_1/` (v1 202, v2 245, v3 1565 = **2012 par**), a amendment jest zweryfikowany maszynowo — `pair_ids_fingerprint` v1 i v2 są **identyczne** z wersją sprzed amendmentu, a jedynym różniącym się polem rekordu jest `policy_id`. Nowy eksport `artifacts/task06/preference_audit_v2/` ma **500 par** (`development_gate_met=true`), orientację 250/250 i 500/500 zobowiązań zweryfikowanych; przy populacji 2012 sampler faktycznie losuje w 18 stratach, więc 1512 par zostaje nieoglądanym zapasem. Eksport 447-parowy jest `superseded`. Zamrożonego kontraktu Groq **nie zmieniono**. Audyt dual-LLM **uruchomiono** (`src/doc2query/preferences/groq_pair_audit.py`, 35 testów CPU): po dniu 1 pokrycie to `gpt-oss` 244/500 i `qwen` 414/500, oba modele odroczone dziennym limitem tokenów, status `incomplete_quota_deferred`, wznawialne jedną komendą. Wyniki wstępne na 244 parach: zgodność automatu z `gpt-oss` **0,701** (CI [0,608; 0,794]) i z `qwen` **0,688** (CI [0,634; 0,743]), ale **między modelami 0,915** (CI [0,854; 0,964]) — niezgodność z porządkiem po marginesie primary nie jest więc szumem sędziów; bramka fail-closed wyklucza **75,4% par** z automatycznej akceptacji; remisy dominują i nie maleją z pewnością sędziego; **~18% par `chosen` uznano za nieodpowiadalne z pasażu** mimo spełnionego round-tripu, a round-trip praktycznie nie różnicuje ocenianej odpowiadalności; pozorny wzrost zgodności z pasmem marginesu **nie utrzymał się** przy większym pokryciu. Progów, rubryki sędziego ani `format.py` nie zmieniono. Raport: `reports/measurements/task06_dual_llm_pair_audit_2026-08-17.md`. Następny krok: dokończyć audyt (128 + 43 requesty) i przeliczyć analizę na 500 parach. Rozszerzenie 2026-08-17 (decyzja właściciela po dniu 1 audytu): kierunek **polityki par v2 zakotwiczonej w osiach defektów** (A: nieodpowiadalność/halucynacja, B: łatwość leksykalna, C: focus; monotonia jawnie poza parami — kontrolki/M-05/GRPO) rozpisany jako specyfikacja `reports/plans/task06_defect_anchored_pairs_v2_spec_2026-08-17.md` z zadaniami V2-00…V2-07; progi zamrożą prospektywne ADR-y po pełnym audycie v1. **V2-00 wykonane**: podaż par defektowych to oś A 17669 grup (naturalna podaż wystarcza), oś B 4857 przy kandydującym cięciu p75, oś C **0** na starych etykietach; `entity_preservation` okazało się w tych kohortach **stałą 1,0 z konstrukcji** (backend scoringu nie ekstrahuje encji), więc oś A opiera się na round-tripie i przyszłym sędzim V2-01. **V2-02 zaimplementowane, kryterium akceptacji niedowiezione**: splitter `focus-v2` eliminuje wszystkie pseudo-zdania, ale abstencja focusa spada tylko o ~1 pp (wąskie gardło: scorer leksykalny, nie segmentacja) — oś C zablokowana do decyzji przy V2-03. **V2-01: harness sędziego odpowiadalności zaimplementowany** (fail-closed pinowanie digestu wag, `temperature=0`, journal z resume, `uncertain` blokuje `chosen` bez liczenia jako defekt, odmowa sędziego z rodziny generatora; kalibracja gotowa na etykietach `answerable_a/b` audytu + klasach korpusu walidacyjnego) — **nie uruchomiony**: maszyna bazowa ma 8 GB VRAM, ollama bez modeli, pobranie Q4 27B (~17 GB) i przypięcie digestu to decyzja właściciela; progi akceptacji zamrozi ADR V2-01 przed kalibracją. Raport: `reports/measurements/task06_defect_inventory_and_focus_v2_2026-08-17.md`. Dokończenie audytu v1 jest **zablokowane operatorsko do 00:00 UTC**: próba wznowienia 2026-08-17 18:20 UTC wykonała **0 requestów**, bo dzienne budżety tokenów obu modeli są wyczerpane (ledgery bez ogona niejednoznacznego, więc `--allow-ambiguous-resend` nie jest potrzebne); ADR V2-03 czeka na pełny wynik, bo jego predykcje muszą mieć baseline z 500 par. Decyzja właściciela 2026-08-17: oś C wypada z pierwszego wydania v2 (wraca w v2.1 po mocniejszym labelerze focusa), konstruowane rejected (V2-04) pomijamy (podaż naturalna wystarcza), a rolę sędziego 27B w osi A miało tymczasowo przejąć **proxy odpowiadalności** skalibrowane na etykietach `answerable_a/b` audytu. Prospektywny ADR `reports/decisions/task06_answerability_proxy_v1.md` zamroził protokół przed policzeniem czegokolwiek wiążącego cechę z etykietą (etykieta = konsensus obu sędziów Groq per strona pary, deterministyczny podział fit/holdout 50/50 po `sha256(audit_id)` z obiema stronami pary w tej samej połowie, przestrzeń 13 zamrożonych cech × 2 kierunki × decyle fit, max 2 atomy, kryterium `precision_yes >= 0,88` **i** `recall_yes >= 0,50`, holdout czytany raz, wiążąca klauzula zastąpienia przez przypiętego sędziego lokalnego). **Proxy nie przeszło** (`src/doc2query/preferences/answerability_proxy.py`, 10 testów CPU): reguła `longest_copied_ngram <= 3 AND pool_positive_score >= 7,777` daje na holdoucie (n=180) czystość **0,8707** (CI [0,816; 0,918]) wobec progu 0,88 przy `recall_yes` 0,9078 — zbicie udziału nieodpowiadalnych z 21,7% do 12,9%, czyli ~40% redukcji zamiast wymaganych ≥45%. Zgodnie z §7 ADR oś A powstanie **bez filtra odpowiadalności**, ADR V2-03 **nie może** przewidywać spadku nieodpowiadalnych `chosen` do 5%, a luka pozostaje nazwanym długiem do ADR-u sędziego lokalnego (harness V2-01 gotowy, brakuje wag 27B). Wynik jest realizacją predykcji zapisanej w ADR przed odczytem i niesie sygnał uboczny wspierający v2: zwycięska reguła używa **absolutnego** `pool_positive_score`, nie marginesu, a przekroju per rola (czystość 0,9024 na `chosen`) świadomie **nie użyto** jako furtki, bo nie był kryterium w ADR. Raport: `reports/measurements/task06_answerability_proxy_v1_2026-08-17.md`. Zmierzono też **baseline monotonii osi D** (`src/doc2query/evaluation/query_monotony.py`, 9 testów CPU, 224 000 zapytań z 11 kohort, wejścia pinowane po SHA-256; zadeklarowane wejście projektowe bez progów i bramek): **monotonia słów początkowych jest dyktowana kontrolką, nie kolapsem modelu** — `intent=procedure` daje `jak` w **100%** (distinct=1, entropia 0,0000 w 8 z 11 kohort), `definition` daje `definicja` w 99,5–100%, więc dwa słowa zbierają połowę populacji, przy `entity_lookup` 816–885 różnych otwarć. Do poprawy są więc dwie kontrolki intencji napisane jak szablony, a nie cel treningowy; M-05 musi raportować słowa początkowe **per `intent`**, inaczej przypisze szablon modelowi. Kontrolka `length` **nigdy nie została użyta** (wszędzie tylko `medium`), więc ciasnoty rozkładu długości nie wolno przypisać modelowi; `form` separuje wyraźnie (6,12 vs 4,06 słowa). Baseline set-level dla nagrody GRPO: distinct-1/2 per grupa 0,470–0,477 / 0,659–0,668 stabilnie w v3–v11, przy v1 niżej (0,326/0,455) — niezależne potwierdzenie kolapsu v1 innym przyrządem. Reaktywacji `entity_preservation` nie wykonano (modele spaCy są na GitHubie, nieosiągalnym z tej maszyny). Raport: `reports/measurements/task06_monotony_baseline_2026-08-17.md`. **Krok 0 zamknięty 2026-08-19: audyt v1 jest kompletny (500/500 par, `status: complete`)** po trzech oknach dziennych budżetów Groq. Zgodność automatu z `gpt-oss` **0,7179** (CI [0,656; 0,779]) i z `qwen` **0,7080** (CI [0,661; 0,755]) wobec **0,8793** między modelami (CI [0,833; 0,925]) — CI nie nachodzą, więc niezgodność z porządkiem marginesowym nie jest szumem sędziów, choć luka jest węższa niż po dniu 1 (16 pp zamiast 22 pp). Bramka fail-closed wyklucza **378/500 (75,6%)**, czyli akceptowalnych jest **122** — o rząd wielkości poniżej bramki 1000 par, co jest twardym argumentem za v2. Remisy dominują (51,8%+9,2% i 32,2%) i są **częstsze** przy wysokiej pewności u obu modeli. Nieodpowiadalne `chosen` **16,6%/18,8%**; round-trip nadal nie różnicuje odpowiadalności. `qwen` płaski w pasmach marginesu (0,693/0,730/0,700), progów nie zmieniono. Slice'y konsensusu pokazują spójny kierunek: im bardziej defektowy `rejected`, tym wyższa zgodność (0,909 `weak_corpus_round_trip` vs 0,797 `lower_primary_margin`) — niezależne wsparcie dla kotwiczenia par w defektach. Baseline dla predykcji V2-03: supports 24,4%, contradicts 6,2%, nieodpowiadalne `chosen` 16,6%/18,8%, remisy 51,8%/32,2%, wykluczone 75,6%. **ADR V2-01 zamrożony** (`reports/decisions/task06_answerability_judge_v1.md`): sędzia **Qwen3.8-27B FP8** na endpoincie vLLM operatora (adres tylko parametrem CLI, nie w repo), kryteria zamrożone przed werdyktami — K1 zgodność z konsensusem Groq (accuracy ≥ 0,85 i balanced accuracy ≥ 0,75 na 817 stronach; baza 0,7846, sufit szumu 0,817), K2 dwustronne sanity klas z konstrukcji (`ungrounded` → `no` ≥ 0,80 przy ≤ 0,20 odrzuceń w klasach dobrych), K3 abstencja ≤ 0,25. Odstępstwo od specyfikacji zapisane jawnie: 8 GB VRAM nie serwuje 27B Q4 uczciwie, zmierzone Q3_K_S działa (3,4 s/item) ale jest gorszym punktem jakościowym i **nie wyprodukowało żadnego werdyktu**; równoległość ollamy nic nie daje (1,03×/1,00×), bo serwer ma jeden slot; ramię z thinkingiem porzucone. Transfer jest **label-free** (1540 itemów, 665 pasaży, `items_sha256 = 31ac2436…`), import fail-closed liczy K1–K3 maszynowo, 11 testów CPU. **Kalibracja wykonana 2026-08-20 i bramka K1–K3 PRZESZŁA**: accuracy **0,8566** (próg 0,85, n=809), balanced accuracy **0,8878** (próg 0,75), `recall_no` 0,9429, `ungrounded` → `no` **180/180**, `good_*` 0,1056/0,1222 odrzuceń (cap 0,20), abstencja **0,0065** (cap 0,25); status `accepted_as_axis_a_answerability_signal`, zero `out_of_schema`. Zapas K1 nad progiem mały (0,66 pp), ale przejście stabilne — drugi niezależny run dał 0,8557. Ten drugi journal dał też samodzielny wynik: **powtarzalność serwera 0,9909** (14 różnic na 1540 mimo `temperature=0` i seeda), czyli continuous batching jest niedeterministyczny. Przegląd ręczny 34 werdyktów (mógł wyłącznie unieważnić — nie unieważnił) wskazuje, że **referencja jest słabszą stroną**: w 8 z 10 rozbieżności sędzia jest bardziej obronny, a w 4 z 6 „nadmiernych odrzuceń” błędna jest etykieta z konstrukcji. Filtr osi A będzie **konserwatywny** (`recall_no` 0,9429 vs `recall_yes` 0,8328) — kosztem podaży, nie czystości. **Paczkowanie zapytań odrzucone bramką A/B**: zgodność **0,9052** wobec progu 0,98 i istotny jednostronny dryf w `uncertain` (21 vs 2 i 19 vs 2, p≈0,0001), przy zerowym zysku wydajnościowym (16,3 it/s paczkami wobec 19,1 pojedynczo) — serwer jest ograniczony dekodowaniem, nie prefillem, co obala też wariant hybrydowy. **Certyfikacja puli wykonana** (172 295 werdyktów, zero kandydatów bez werdyktu, zero `out_of_schema`) i **podaż osi A zmierzona**: **2 253 pary z kohort autoryzowanych** (62,3% z 3 619 grup) i 13 736 z v4–v11, razem **15 989**. To zdejmuje najmocniejszy zarzut wobec v1.1 — tam po bramce audytu zostawało 122 pary akceptowalne wobec progu 1000. Filtr sędziego zachowuje **79,5%** czystych `chosen` (spójnie 75,5–80,1% w kohortach), co jest zmierzonym kosztem jego konserwatywności; naturalny `rejected` osi A istnieje w **96,9%** grup, więc konstruowane rejected (V2-04) są zbędne — teraz na zwalidowanym sygnale, nie na round-tripie. Bramka różnorodności odrzuciła jedną grupę. Raport: `reports/measurements/task06_axis_a_supply_after_certification_2026-08-20.md`. Raport: `reports/measurements/task06_answerability_judge_v1_2026-08-20.md`. **ADR V2-03 zamrożony 2026-08-20 przed zbudowaniem pierwszej pary v2** (`reports/decisions/task06_defect_pair_policy_v2.md`, config `configs/preferences/task06_defect_pair_policy_v2.yaml`, moduł `src/doc2query/preferences/pair_policy_v2.py` i eksport `pair_audit_export_v2.py`, 32 testy CPU): osie **A i B** (oś C poza wydaniem, więc słaby filtr focusa z v1 **zdjęto** — etykiety focusa są zmierzenie zepsute), definicje bajtowo zgodne ze zmierzoną podażą 2 253 par, cięcie osi B **p75 `content_jaccard ≥ 0,0857`** dla `rejected` i **p50 ≤ 0,0556** dla `chosen` (p90 odrzucone: przy filtrze sędziego 79,5% i ograniczeniu do 13,9% grup groziło zejściem poniżej kwoty 250 par osi B), kwoty **250/250** z realokacją i jawnym niedoborem, deterministyczne przypisanie osi haszem grupy (kolejność prób, nie licznik), **tie-break DivPO przyjęty**, margines primary wyłącznie jako sanity `pool_margin > 0` (`margin_used_for_ordering=false`, pasma marginesu usunięte ze strat), **veto shadow zdjęte** z uzasadnieniem i z zapisem, że wzrost liczby par względem v1 nie jest miarą jakości, konstruowane rejected pominięte (cap 0%), werdykty z journala przypiętego po SHA-256 (brak werdyktu przerywa run). Predykcje wiążące bramki V2-05: P1 nieodpowiadalne `chosen` ≤ 5% (wyprowadzone z `recall_no` 0,9429: reszta punktowa 1,01%), P2 `consensus_supports_automatic` ≥ 30% z dolną granicą CI powyżej 24,4%, P3 `consensus_contradicts_automatic` ≤ 3,1%, P4 kontrast osi ≥ +20 pp jako test mechanizmu; remisy bez progu. **Pary v2 zbudowane i ślepy eksport zamrożony** (2026-08-20, po commicie zamrażającym ADR): **2 278 par** z kohort autoryzowanych (v1 204, v2 274, v3 1 800; 62,9% z 3 619 grup `eligible`), **oś A 2 086 / oś B 192**, zero kandydatów bez werdyktu, a kontrola zgodności wychodzi dokładnie (2 278 = zmierzone 2 253 pary osi A + 25 grup parowalnych wyłącznie na osi B; rozkład werdyktów identyczny z pomiarem podaży). To ponad dwukrotność progu 1 000 par, ale **liczba par nie jest miarą jakości** — v2 zdjęła też veto shadow, którego cena jest wyceniona: `shadow_agrees` w 73,1% par, czyli v1 unieważniłaby 613 par (26,9%). **Degradacja marginesu jest zmierzona**: 617 par (27,1%) ma `primary_margin_delta < 0`, czyli `chosen` o niższym marginesie niż `rejected` — v1, wymagająca przewagi ≥ 1,0, nie zbudowałaby żadnej z nich. **Wynik negatywny: oś B nie dowiozła kwoty 250 — dała 192**, i to nie z powodu cięcia overlapu, ale zamrożonej reguły przypisania osi (hasz daje kolejność prób, więc często parowalna oś A absorbuje większość grup parowalnych na obu osiach; 954 pary, 41,9%, powstały na osi zapasowej). Reguły, cięć ani kwot nie zmieniono — zadziałała prerejestrowana realokacja (oś A 308, oś B 192), więc próbka ma 500 par, `development_gate_met=true`, a P4 będzie mierzone na 192 parach osi B. Etykieta `possible_ambiguous_query` jest praktycznie stałą (99,6%), więc zapisano przed audytem, że nie nadaje się na wymiar slice'owania. Ślepy eksport w nowym katalogu `artifacts/task06/preference_audit_v3_defect_pairs/`: 500 par, 12 strat (`cohort_id × axis × requested_form`, bez pasm marginesu), orientacja 250/250, 500/500 zobowiązań zweryfikowanych, dokładnie pięć dozwolonych pól ślepych i zero wycieku; eksporty v1/v2 nietknięte. Audytu v2 **nie uruchomiono**, predykcje pozostają nieodczytane. Raport: `reports/measurements/task06_defect_pairs_v2_2026-08-20.md`. **Dług czytnika domknięty amendmentem 2026-08-21** (`reports/decisions/task06_groq_audit_reader_axis_amendment_2026-08-21.md`), spisanym przed pierwszym requestem audytu v2: właściciel autoryzował adaptację czytnika zamiast duplikatu pliku, pod warunkiem zera zmian w promptcie, rubryce, modelach, limitach i regułach decyzyjnych. Przegląd każdego użycia brakujących pól potwierdził, że wchodzą wyłącznie do opisowych tabelek, nie do ocen, werdyktów, bramek ani predykcji. Zmiany są trzy i mechaniczne: dyspozytor manifestu po kontrakcie eksportu, wymiar przekroju `primary_margin_gap_band` (v1) → **`axis`** (v2) oraz nazwa pola etykiet `rejected_failure_types` → `rejected_defect_labels`. Klucze analizy idą za kontraktem eksportu, więc ścieżka v1 pozostaje odtwarzalna — wszystkie stare klucze zachowują nazwy i wartości, dochodzi jeden nowy klucz opisowy `export_contract`. 38 testów pliku audytu, w tym trzy nowe; oba rzeczywiste eksporty ładują się właściwym modelem. **Audyt v2 ukończony 2026-08-23** (500/500 par, 250/250 requestów u obu sędziów, `status: complete`, dwa okna budżetów Groq, bez zmiany promptu, rubryki, modeli i limitów): **bramka V2-05 niedowieziona — dwie z czterech wiążących predykcji nie przeszły**. P1 (nieodpowiadalne `chosen` ≤ 5% u każdego sędziego) dało `gpt-oss` **4,80%** (PASS) i `qwen` **5,20%** (FAIL); P3 (`consensus_contradicts_automatic` ≤ 3,1%) dało **3,20%** (FAIL). Obie porażki są **o jedną parę** (25/500 i 15/500 by przeszły), a CI obu wielkości zawiera próg — co tnie w obie strony i pokazuje, że n=500 nie ma mocy rozstrzygać na tym poziomie; progów **nie** obniżono, audytu **nie** powtórzono. P2 przeszło (**30,80%**, CI [26,78%; 35,05%] wobec progu 30% i bariery 24,4%), P4 przeszło z ogromnym zapasem (**+45,9 pp** i **+56,7 pp** wobec progu +20 pp). Zgodnie z §11 ADR pary v2 **nie zastępują niczego i nie idą do żadnego treningu; polityka wraca do projektowania**. Jednocześnie v2 poprawiła **każdy** mierzony wymiar wobec v1: nieodpowiadalne `chosen` 16,6%/18,8% → **4,8%/5,2%** (3,5×, niezależne potwierdzenie skuteczności przypiętego sędziego odpowiadalności), zgodność z automatem 0,718/0,708 → **0,886/0,778**, zgodność między modelami 0,879 → **0,983** (więc reszta nie jest szumem sędziów), wykluczone 75,6% → **69,2%**, konsensus wspiera 24,4% → **30,8%**, przeczy 6,2% → **3,2%**. Mechanizm osi defektowych jest potwierdzony ponad wątpliwość: nieodpowiadalne `rejected` to 49,0% w osi A wobec 3,1% w osi B (`gpt-oss`) i 63,0% wobec 6,3% (`qwen`). **Oś B jest słabym ogniwem**: zgodność konsensusu z automatem wynosi 0,974 w osi A (n=154) wobec **0,250** w osi B (n=16), a etykieta `high_lexical_overlap` też 0,250 — łatwość leksykalna nie jest dla sędziów powodem uznania zapytania za gorsze, co przy niedoborze podaży osi B (192 wobec kwoty 250) podważa samą hipotezę tej osi, choć liczby są małe. Remisy wzrosły u `gpt-oss` (63,2% wobec 51,8%+9,2%), u `qwen` bez zmian (34,2%); abstencja konsensusu 65,4%. Wynik jest negatywny wobec bramki, nie wobec kierunku; decyzja o v2.1 albo o przeprojektowaniu reguły decyzyjnej należy do właściciela i wymaga nowego prospektywnego ADR **przed** kolejnym odczytem. To nie jest human evidence. Raport: `reports/measurements/task06_defect_pairs_v2_audit_2026-08-23.md`. **Diagnostyka post hoc porażki (eksploracyjna, nie zmienia werdyktu)**: obie porażki pochodzą z osi B — P1 wynosi w osi A 3,90%/3,25%, a w osi B 6,25%/8,33%; P3 wynosi w osi A **1,30%**, a w osi B 6,25%, czyli oś B wnosi trzy czwarte wszystkich sprzeczności przy 38,4% próbki. To trzeci niezależny sygnał przeciw osi B (obok zgodności 0,250 i niedoboru podaży) i przesuwa podejrzenie z cięcia `content_jaccard` na samą hipotezę, że wyższy overlap jest defektem. Diagnostyka **nie** ustala, że polityka jednoosiowa przeszłaby bramkę (górna granica CI dla P1 osi A sięga 6,71%) i **nie** usuwa wady konstrukcyjnej predykcji: progi punktowe przy n=500 mają przedziały ~±2 pp, więc v2.1 musi ustalać próg razem z liczebnością próby. Raport: `reports/measurements/task06_v2_gate_failure_decomposition_2026-08-23.md`. **ADR polityki par v2.1 zamrożony 2026-08-23 przed zbudowaniem pierwszej pary v2.1 i przed pierwszym nowym requestem Groq** (`reports/decisions/task06_defect_pair_policy_v2_1.md`, dokument-only; kodu, par ani audytu nie tknięto). Rozstrzyga trzy rzeczy, każdą liczbą. **(1) Oś B wypada z polityki** — trzy niezależne zmierzone sygnały przeciw (zgodność konsensusu 0,250 przy n=16 wobec 0,974 przy n=154, podaż 192 wobec kwoty 250, trzy czwarte sprzeczności P3 przy 38,4% próbki), a naprawą **nie** jest przestawienie cięcia `content_jaccard`, bo podejrzenie pada na samą hipotezę osi. v2.1 jest jawnie **jednoosiowa**: uczy wyłącznie „odpowiadalne i ugruntowane > nieodpowiadalne", więc nie naprawi łatwości leksykalnej, monotonii, focusa ani naturalności — to zapisane jako ograniczenie, nie uproszczenie. Znikają hasz przypisania osi, kwoty 250/250, realokacja i warunek `content_jaccard ≤ 0,0556` na `chosen` (istniał tylko dla rozdzielności osi B), więc definicja `chosen` wraca **bajtowo** do tej, na której zmierzono podaż 2 253 par. Łatwość leksykalna wraca do kontrolek/M-05/GRPO, gdzie baseline monotonii wskazał jej prawdziwe źródło (`intent=procedure` → `jak` w 100%, kontrolka `length` nigdy nieużyta), a powrót osi B to nazwany dług wymagający **walidacji etykiety** na wzór bramki K1–K3, nie nowego cięcia. Różnorodność wewnątrz osi A jest realna i wchodzi do strat próbki jako etykieta defektu (`judge_unanswerable` n=142/0,993, `weak_corpus_round_trip` 69/0,957, `judge_rank_disagreement` 48/1,000). **(2) Progi punktowe zastąpiła reguła przedziałowa** ustalona **razem** z liczebnością: dokładny jednostronny 95% przedział Cloppera–Pearsona, werdykt trójwartościowy PASS/FAIL/`INCONCLUSIVE`, przy czym `INCONCLUSIVE` jest fail-closed i rozdziela „polityka jest zła" od „przyrząd nie miał rozdzielczości". Progi **nie zostały zmienione** — reguła jest surowsza od v2 (wymaga całego przedziału pod progiem), a ceną jest próba; koniunkcja przekroju–unii nie wymaga korekty na wielokrotność; nie ma eskalacji ani dolewania par. P2 zaostrzono (dolna granica ≥ 30%, nie punkt ≥ 30%) bo przy założeniu osi A 48,7% jest to darmowe (moc 1,000, punkt decyzyjny 262/800); P3 konfirmacyjnie ≤ 16/800 = 2,00% (moc **0,964** przy założeniu 1,30%); P4 **musiało** zostać przedefiniowane, bo kontrast **między** osiami w polityce jednoosiowej nie istnieje — P4' mierzy ten sam mechanizm sparowanie wewnątrz pary (nieodpowiadalne `rejected` − `chosen`, próg +20 pp bez zmian, założenie +45,1/+59,8 pp, 13,2 i 21,9 sd zapasu, percentylowy bootstrap po parach, ziarno 20260823). **P1 schodzi z roli konfirmacyjnej do guardraila** — jedyne osłabienie ciężaru dowodu w tym ADR, opisane wprost: próg 5% zostaje nietknięty, ale FAIL wymaga **dowiedzionego** naruszenia (dolna granica > 5%, czyli ≥ 51/800 = 6,375%). Powód jest policzony, nie retoryczny: przy prawdzie 3,90% (oś A, `gpt-oss`) reguła konfirmacyjna wymaga n=1 103 dla mocy 0,50, **n=2 310 dla 0,80** i 3 147 dla 0,90, a przy prawdzie 4,80% (cała próbka v2) próg 5% jest nieosiągalny przy **żadnym** n; jednocześnie wyprowadzenie progu (reszta 1,01% z `recall_no` 0,9429, „5× zapasu") zostało sfalsyfikowane pomiarem 3–5× wyższym, a to, co P1 miało potwierdzać, ma niezależne potwierdzenie (bramka K1–K3 sędziego oraz zmierzony 3,5× spadek przy zgodności między sędziami 0,983). Charakterystyka guardraila zapisana z góry: P(FAIL) = 0,001 przy prawdzie 3,90%, 0,048 przy 5,0%, 0,577 przy 6,5%, 0,964 przy 8,0% i 1,000 przy poziomie v1 — łapie regresję materialną, nie 1 pp, i tak ma być raportowany (zabronione pisanie „P1 przeszła", gdy guardrail tylko się nie zapalił). Moc łączna części konfirmacyjnej **0,964**, więc ryzyko `INCONCLUSIVE` wycenione na ~3,6%; analiza wrażliwości zapisana przed odczytem (P3 przy prawdzie 2,00% spada do 0,566 — to jedyna predykcja realnie zagrożona nierozstrzygnięciem). **(3) Próba: 800 par bramki + 300 par kotwicy złotej = 1 100** z podaży 2 086 par osi A w kohortach autoryzowanych, czyli ~53% populacji, z **co najmniej 900 parami nieoglądanymi w zapasie** (próbka wyczerpująca populację nie zostawia materiału na przyszły test prospektywny). Kohorty v4–v11 **pozostają zamknięte** — wybrana próba ich nie potrzebuje. Koszt policzony z ledgerów zamkniętego audytu v2, nie z założeń: `gpt-oss` zużył 235 348 tokenów na 500 par = **470,7 tok/para** (`qwen` 373,3) przy bezpiecznym limicie 185 000 tok/dobę, czyli **393 pary na okno**; 1 100 par to 2,80 token-doby → **3–4 okna** (kontrola metody: n=500 to 1,27 token-doby i zużyło faktycznie 2 okna). Wariant z P1 konfirmacyjnym kosztowałby 6–7 okien i autoryzację nowych kohort przy mocy łącznej 0,82. Komórka kotwic (naturalne zapytanie `train` vs `chosen` z grup rozłącznych z bramką, ta sama rubryka) jest **niebramkowa** i ma jeden cel: zmierzyć podłogę sędziów na złotym zapytaniu, żeby przyszły próg absolutny wyprowadzać z pomiaru, a nie z modelu reszty — przy n=300 obserwowane 4% daje CP [2,32%; 6,40%]. Nie relabeluje ani nie filtruje żadnej pary naturalnej, nie dotyka progu `source_en_score`, nie jest human evidence. Prerejestrowana ścieżka niedoboru: bramka ma priorytet do 800, kotwice minimum 200 albo odroczenie, przy <800 parach moc przeliczana i raportowana **przed** odczytem, przy <500 bramka nie startuje, poluzowanie progu zabronione. Bramka V2-05 pozostaje przegrana i nie jest przeliczana; audytu v2 nie powtarzamy. **Wykonanie v2.1 rozpoczęte 2026-08-23, po commicie zamrażającym ADR**: polityka i eksport zaimplementowane w nowych modułach (`pair_policy_v2_1.py`, `pair_audit_export_v2_1.py`, 21 testów CPU; zamrożony pipeline v2.0 nietknięty), czytnik audytu dostał trzeci adapter kontraktu (wymiar opisowy = etykieta defektu zamiast osi), a amendment `task06_groq_audit_sample_size_amendment_2026-08-23.md` — spisany **przed pierwszym requestem** — zamienił twardą bramkę `pair_count == 500` z waiveru pilota na zbiór liczebności zamrożonych ADR-ami `{500, 800}`, nadal fail-closed (501 odrzucane testem), bez zmiany promptu, rubryki, modeli, `batch_size`, limitów, retry i quota_schedulera. **Budowa par dała 2 253 pary** (v1 199, v2 270, v3 1 784 z 3 619 grup `eligible`; `judge_unanswerable` 1 704, `weak_corpus_round_trip` 549) — czyli **dokładnie** zmierzoną wcześniej inną ścieżką kodu podaż osi A po certyfikacji, co jest niezależną kontrolą, że zdjęcie osi B przywróciło definicję `chosen` bajtowo (v2.0 miała na osi A 2 086, bo 167 grup zabrał hasz osi). Liczba par nie jest miarą jakości. **Ślepy eksport komórki bramkowej zamrożony**: 800 par z 2 253, 12 strat, ziarno 20260823, orientacja 400/400, 800/800 zobowiązań zweryfikowanych, dokładnie pięć dozwolonych pól ślepych i zero wycieku, 800 unikalnych grup i klastrów pasaży, etykiety w próbce 606/194, **1 453 pary zostają nieoglądane w zapasie**; katalog `preference_audit_v4_defect_pairs_v2_1/` jest nowy, eksporty v1/v2/v3 nietknięte. **Pierwsze okno audytu uruchomione** (plan 800 requestów, 400 na model, 1 600 ocen; run zdrowy, ~954 tok/request u `gpt-oss` wobec 941 zmierzonych w v2, zero zdarzeń błędu) i zatrzyma się czysto jako `incomplete_quota_deferred` po wyczerpaniu dziennego budżetu (~390 par na okno), wznawialny tą samą komendą. **Żaden wynik nie został odczytany**; bramka V2.1-05 policzy się dopiero po `status: complete`. Komórka kotwic (300 par, niebramkowa, rozłączna) powstanie po starcie audytu przez `--exclude-export-dir`. Raport: `reports/measurements/task06_defect_pairs_v2_1_build_2026-08-23.md`. **Audyt v2.1 ukończony 2026-08-25** (`complete`, 400/400 requestów u obu sędziów, 800 par, 1600 ocen, trzy okna budżetów Groq, bez zmiany promptu, rubryki, modeli i limitów), a **bramka V2.1-05 policzona modułem napisanym przed odczytem** (`defect_pair_gate_v2_1.py`, odmawia niedokończonego audytu): **niezdana, blokuje P3 ze statusem `INCONCLUSIVE`**. P3 dało 17/800 = **2,12%** z górną granicą CP **3,1704%** wobec progu 3,1% — brakuje **0,0704 pp**, czyli znowu **jednej pary** (16/800 dałoby 3,0219% i PASS). Różnica wobec v2.0 jest jednak zasadnicza i po to reguła była przepisana: v2.0 orzekło FAIL na podstawie punktu, twierdząc że polityka jest zła, a v2.1 orzeka INCONCLUSIVE, czyli że **ta próba nie rozstrzyga tego progu**. P2 przeszło pewnie (**45,12%**, CI [42,19%; 48,09%] wobec progu 30% — więcej niż 30,80% w v2.0 i 24,40% w v1), P4' przeszło z ogromnym zapasem u obu sędziów (**+38,2 pp** i **+55,5 pp** wobec +20 pp), a guardrail P1 **nie zapalił się** (41/800 = 5,12% i 46/800 = 5,75%, dolne granice 3,91% i 4,46%) — ale **nie wolno tego raportować jako „P1 przeszła"**: punkty leżą powyżej progu 5%, a moc konfirmacyjna przy n=800 to 0,39/0,76. **To był scenariusz zapisany z góry**: ADR §4.6 przewidział, że P3 jest jedyną predykcją realnie zagrożoną nierozstrzygnięciem i że taki wynik będzie sygnałem, iż wartości osi A z przegranej bramki v2.0 były optymistyczne przez selekcję podpróby. Tak było na obu wymiarach: P3 założone 1,30% (moc 0,964) wobec zmierzonych 2,12% (moc **0,467**), P1 założone 3,90%/3,25% wobec zmierzonych 5,12%/5,75%. Zgodnie z §4.4 pary v2.1 **nie idą do żadnego treningu**, progów nie zmieniono, audytu nie powtórzono, próbki nie rozszerzono. Wejście projektowe do decyzji właściciela: przy zmierzonej prawdzie 2,125% moc 0,80 dla P3 wymaga **1 760 par** (≤42 sprzeczności, 4–5 okien Groq), a 0,90 wymaga **2 360 par** (6–7 okien) — przy populacji 2 253 par, z czego 800 już oglądanych, więc nieoglądany zapas to **1 453 pary** i eskalacja jest zakazana; opcje to pomiar na całym zapasie (moc ~0,74), rozszerzenie kohort v4–v11 (zamknięte do pozytywnej bramki, więc wymaga świadomego zdjęcia warunku) albo przeprojektowanie samej predykcji P3. **P1 jest już definitywnie nierozstrzygalny konfirmacyjnie**: przy prawdzie 5,12% i progu 5% żadne n nie da przejścia, więc guardrail pozostaje jedyną sensowną formą tej predykcji. Raport: `reports/measurements/task06_v2_1_gate_result_2026-08-25.md`. **Kierunek v3 zamrożony 2026-08-25 decyzją właściciela** (`reports/decisions/task06_judge_selected_pair_policy_v3.md`, ADR prospektywny spisany **przed pierwszym wywołaniem selektora i przed pierwszą parą v3**): zamiast trzeci raz przestawiać poprzeczkę, zmienia się **pytanie** — o lepszej stronie pary orzeka **lokalny sędzia `Qwen3.8-27B` FP8 porównujący zapytania wprost**, a nie złożenie sygnałów pośrednich. To jest zmiana roli, którą AGENTS.md §7 wymienia jako zakazaną „bez prospektywnego ADR", więc ten ADR nią jest; config sędziego v1 (`used_for_pair_building: false`) zostaje nietknięty, a v3 ma osobny kontrakt z `used_for_pair_selection: true`. **Amendment wykonawczy do zapisu o przepustowości**: zakaz masowego scoringu powstał dla 27B w Q4 przez ollamę z offloadem na 16 GB, a operator ma serwer FP8 vLLM **bez limitów dobowych** o zmierzonych **19,1 itemu/s** — ograniczenie sprzętowe przestało obowiązywać, adres endpointu pozostaje wyłącznie parametrem CLI, wagi przypięte digestem. Projekt: ślepość (pasaż + dwa zapytania, zero score'ów i zero informacji o wyborze automatu), **obowiązkowa zamiana pozycji** w obu kolejnościach z `position_flip` jako przypadkiem nierozstrzygniętym, **trzy rubryki z definicjami i jawną hierarchią konfliktu** (ugruntowanie > użyteczność > naturalność > długość; kopiowanie dyskwalifikuje) oraz **dozwolone rozumowanie** — w przeciwieństwie do chudej rubryki Groq, której prawdopodobnym skutkiem było 47,5% remisów i 401 abstencji na 800 par. Selekcja jest trzyetapowa: tanie guardy dopuszczalności (format, lead-in, a dla `chosen` round-trip@20 i brak `copy_risk`; werdykt odpowiadalności **przestaje decydować o rolach** i zostaje etykietą), turniej rankingujący rubryką R3 z zamianą pozycji, a na finałowej parze pełny ensemble sześciu głosów. Wybrany wariant kosztuje **86 856 wywołań, czyli 1,3 h bez rozumowania i 3,8 h z rozumowaniem** (pełny ensemble wszędzie kosztowałby 195 426 wywołań i 2,8/8,5 h). Strona `rejected` dostaje **jedną prerejestrowaną ablację**: `bottom` vs `near_miss`, oba warianty zapisywane osobno, żaden promowany bez pomiaru. **Próg agregacji głosów NIE jest tu zamrożony** — zamrozi go amendment po kalibracji i przed pierwszą parą, bo ustalanie go teraz powtarzałoby błąd P3 z v2.1, gdzie próg wziął się z arytmetyki, nie z pomiaru. Kalibracja idzie na **korpusie walidacyjnym nagrody** (1 440 zapytań o etykietach znanych z konstrukcji) i mierzy czystość per rubryka, obciążenie pozycyjne, krzywą czystość/wydajność dla reguł 6/6, 5/6 i 4/6 oraz udział remisów; kontrolnie na 1 600 ocenach Groq z 800 spalonych par v2.1. Obowiązkowe raportowanie efektu ubocznego: filtrowanie do par pewnych podnosi precyzję i **obniża trudność**, więc raport musi pokazać przeżywalność par na każdym poziomie surowości i rozkłady wobec v2.1 — bez tego nie wolno twierdzić, że filtr poprawił dane. Groq nie znika i nie zmienia kontraktu: audytuje selektor v3 na parach **nieoglądanych** (zapas 1 453), z predykcjami wyprowadzonymi z kalibracji i zamrożonymi osobnym ADR, przy zachowanej regule przedziałowej Cloppera-Pearsona z `INCONCLUSIVE` fail-closed. **Pary v2.1 przestają być kandydatem na dane treningowe** i stają się materiałem kalibracyjnym; wynik bramki V2.1-05 pozostaje zapisany jak jest, bez reinterpretacji i bez zmiany progów. Kohorty v4–v11 nadal zamknięte. **Selektor v3 zaimplementowany i skalibrowany 2026-08-27** (`pair_selector_v3.py`, 22 testy CPU; runner `scripts/run_v3_calibration.sh` na serwerze operatora, 1 800 par o kierunku znanym z konstrukcji, 3 rubryki × 2 kolejności = 10 800 wywołań na ramię, `temperature=0`). Wykonanie: ramię **bez rozumowania** 10 800 wywołań, **zero porażek**, 5,81/s, 31 min; ramię **z rozumowaniem** 10 498 wywołań, **302 porażki (2,8%)**, 0,44/s, **405 min**, 232 pary utracone na obcięciu budżetu 1 024 tokenów. **Pierwszy odczyt był mylący i został skorygowany własną analizą**: zagregowana czystość przy jednomyślności 6/6 wyszła 0,8498, co wyglądało na wniosek, że spójność sędziego nie informuje o poprawności — ale rozbicie per klasa pokazało dwie różne populacje. `ungrounded` **1,0000** (265 par), `copy_verbatim` **1,0000** (227), `too_general` **0,9433** (282), natomiast `wrong_form` 0,8099 i `wrong_focus` **0,3704**, czyli **poniżej losowego**. Te dwie ostatnie klasy są zepsute **względem kontrolki, której ślepy sędzia nie widzi** (żądany fragment i żądana forma), więc są nierozstrzygalne z definicji — a czystość na nich jest **płaska wobec progu** (0,619/0,628/0,617 dla 2/4/6 głosów), co jest sygnaturą błędu systematycznego, nie szumu. To samo tłumaczy pozorną słabość rubryki ugruntowania: R1 ma 0,871 na klasach rozstrzygalnych i **0,521**, czyli poziom losowy, na nierozstrzygalnych, bo tam ugruntowanie obu stron jest identyczne. Krzywa właściwa (klasy rozstrzygalne, 1 080 par, bez rozumowania): ≥2 głosy wydajność 0,981 przy czystości 0,9547; ≥4 głosy 0,956 przy 0,9584; **≥6 głosów 0,717 przy 0,9793**. Obciążenie pozycyjne jest realne i rzędu 10% (R1 0,124, R2 0,091, R3 0,097), co uzasadnia obowiązkową zamianę pozycji. Raport: `reports/measurements/task06_v3_selector_calibration_2026-08-27.md`. **Amendment progu zamrożony po odczycie i przed pierwszą parą v3** (`reports/decisions/task06_v3_selector_aggregation_amendment_2026-08-27.md`): reguła to **jednomyślność 6/6**, wybrana nad 4/6 mimo utraty jednej trzeciej par, bo 4/6 zostawia 4,2% par o odwróconym kierunku wobec 2,1% przy 6/6, a objętość jest w tym programie rozwiązywalna inaczej (pula osi A ma 15 989 kandydatów) niż czystość. **Zakres ważności zamrożony wąsko**: selektor jest zwalidowany dla ugruntowania, kopiowania i ogólności, a **nie jest i nie będzie** sędzią zgodności z formą i focusem — oś C zostaje poza wydaniem, teraz z pomiarem zamiast argumentu o zepsutym etykieciarzu, a powrót wymagałby sędziego **widzącego kontrolkę**, czyli innego kontraktu. **Rozumowanie odrzucone**: +0,8 pp czystości przy 6/6 za 13× koszt i 2,8% porażek. Rubryki zostają wszystkie trzy, mimo że R3 jest najczystsza — usunięcie R1 i R2 po zobaczeniu liczb byłoby dobieraniem rubryk pod wynik, czego §10 ADR zakazuje, a to jednomyślność trzech daje 0,979 wobec 0,936 dla samej R3. **Pary v3 zbudowane 2026-08-27.** Turniej u operatora: 3 619 grup w pakiecie (chudy, 1,1 MB spakowany, bez score'ów — manifest nosi `contains_scores=false`), **537 grup pominiętych** przed pierwszym porównaniem, bo nie mają ani jednego czystego kandydata, **3 082 sparowane**, **86 007 wywołań** w 3,7 h przy 6,42/s i **5 porażek** (0,006%). Wywołań mniej niż szacowane 101 012, bo cache porównań odsiał pary powtarzające się między przebiegiem „najgorszy" i „drugi od końca". Jednomyślność 6/6: **2 902 grupy (94,2%)** dla wariantu `bottom` i **2 575 (83,6%)** dla `near_miss`. Wewnętrzna sprzeczność selektora (ranking mówi A, pełny ensemble jednomyślnie B) wynosi **0,42%** dla `bottom` i **1,9%** dla `near_miss` — zachowuje się dokładnie jak powinna, bo pary near-miss są bliższe jakością, i jest niezależnym potwierdzeniem, że liczby nie są przypadkowe. **Złożone pary: 2 730** (`bottom`) i **2 428** (`near_miss`), oba warianty powyżej 2 253 par v2.1, przy średnim `normalized_query_jaccard` 0,1832 i 0,2261 (near-miss faktycznie dobiera bliższe kandydatury, czyli robi to, co ma robić jako ablacja `top-vs-near-miss`). **Znaleziony i naprawiony błąd własny**: 165 grup (`bottom`) i 141 (`near_miss`) padło na `chosen_failed_reverification`, bo eksport pakietu liczył czystość **słabszą** definicją niż etap składania — pomijał `pool_margin > 0` i `entity_preservation`. Zbudowane pary są poprawne, bo składanie było stroną surowszą (błąd kosztował podaż, nie czystość), strata jest policzona (5,4% sparowanych), a naprawa strukturalna: obie ścieżki wołają teraz jedną funkcję `_clean_chosen` z polityki i test tego pilnuje. Turnieju **nie przeliczam** — odzyskanie 165 grup kosztowałoby 4 h serwera i nie zmieniłoby wniosków przy 2 730 parach. Każdy rekord pary nosi `form_or_focus_compliance_claimed=false`. Raport: `reports/measurements/task06_v3_pairs_2026-08-27.md`. **Amendment zdejmujący Groq z roli bramki dla v3** (`reports/decisions/task06_v3_groq_role_amendment_2026-08-27.md`, spisany przed jakimkolwiek audytem par v3, decyzja właściciela): gęstość sygnału w audytach Groq to **30,6%** rozstrzygniętych w v2 i **47,2%** w v2.1, czyli trzy doby zegarowe za 150–380 informatywnych porównań, a wartość krańcowa spadła (v1 zabił porządkowanie marginesem, v2 zabił oś B, v2.1 tylko potwierdził znane i zablokował się na progu bez wyprowadzenia z pomiaru). Etykiety z konstrukcji są dowodem mocniejszym niż zgoda innego modelu i dały 0,9793 w 31 minut bez limitów. Zamiast bramki obowiązują trzy rzeczy w kolejności ważności: **probe embedder jako kryterium rozstrzygające** (AGENTS.md §9.2), walidacja selektora na etykietach z konstrukcji (wykonana) i **ślepy spot-check właściciela na 50 parach** jako kontrola operacyjna, jawnie **nie** będąca panelem §9.3. Groq zostaje przewidziany dla zamrażania finalistów, ale z zapisanym warunkiem wstępnym naprawy rubryki, która produkuje ponad połowę abstencji. Kontrakt Groq, zamknięte audyty i zapas 1 453 nieoglądanych par v2.1 pozostają nietknięte. Następny krok: handoff par v3 do Task 07 (train/dev, continued-SFT, wagi), token lengths na prawdziwych parach i spot-check właściciela; potem DPO i probe. `task07_training_authorized=false`, `final_tests_used=[]`. |
| [07](07_dpo_training.md) | DPO i continued-SFT control | `IN PROGRESS` | D01 controlled 4.5B pozostaje jedynym przyszłym startem DPO, W06 anchor/source bez łączenia wag. Task 06 ma dopiero fail-closed execution design, nie dane ani osobną politykę chosen/rejected; `task07_training_authorized=false`, a smoke/PEFT, trzy ramiona, seedy i ewaluacja pozostają niewykonane; `final_tests_used=[]`. Rozszerzenie 2026-08-13: Task 07 jest jawnie także destylacją selektora; obowiązkowa ewaluacja trybu produkcyjnego bez selektora (M-05) dla finalistów. **Runtime DPO powstał 2026-08-24** (`src/doc2query/training/dpo_runtime.py`, 14 testów CPU): dotąd `training/dpo.py` zawierał wyłącznie kontrakty, walidatory i skalarną stratę, a `DPOTrainer` **nie występował nigdzie w `src/`** — `doc2query train dpo` pozostaje stubem. Architektura jest **dwufazowa**, bo tak wymagają istniejące kontrakty: precompute logprobów referencji (model zamrożony, wznawialny journal, odmowa datasetu w innej kolejności, weryfikacja SHA-256 pliku logprobów) i dopiero potem trening polityki klasyczną stratą sigmoid DPO na precomputowanych logprobach. `trl.DPOTrainer` **świadomie odrzucony**: w TRL 0.29.1 przy modelu PEFT i `ref_model=None` referencją jest baza z wyłączonym adapterem, czyli **nie** punkt startowy, a Task 07 wymaga wprost walidacji, że referencja odpowiada punktowi startowemu; drugi pełny model nie mieści się obok polityki na 8 GB. Completion nigdy nie jest ucinany (nadmiar leci z lewej strony promptu, fakt ucięcia raportowany), a torchowa strata jest w teście porównana ze zamrożoną funkcją skalarną. **Memory probe zmierzony na prawdziwym stosie** (baza 4.5B NF4 + adapter `D01-4.5B-STYLE-50K-S42`, RTX 3060 Ti, pary syntetyczne — bez par v2.1 i bez bramki): peak **2,71 GiB** (precompute) i **3,78 GiB** (trening) przy `max_length=512`, oraz **2,78 / 4,14 GiB** przy 768, czyli **o połowę mniej niż 7,74 GiB peaku SFT 4.5B**, bo faza treningowa nie trzyma drugiego modelu. Koszt: 0,711 s/para precompute i 2,599 s/krok przy 512 (0,861 i 3,697 przy 768), co dla 2 253 par daje ~27 min precompute i ~1,6 h jednej epoki; przy trzech ramionach trening to rząd 5–7 h, a **kosztem dominującym pozostaje ewaluacja probe** (~1500 s/run × ≥5 par seedów + 18,5% reseedów ≈ 7,5 h), więc to ona wyznacza okno GPU. Raport: `reports/measurements/task07_dpo_memory_probe_2026-08-24.md`. Handoff par v3 jest spakowany (2 461 train / 269 dev), a długości tokenowe zmierzone. 2026-08-28 zamrożono plan model-free `task07-dpo-plan-v3-bottom-s42` (`max_length` 768 / prompt 704 z pomiaru, 154 kroki, budżet 1 087 057 tokenów, tożsamość stosu policzona z treści plików) i wykonano **precompute logprobów referencji na prawdziwych parach: 2 461/2 461, zero uciętych promptów, peak 2,72 GiB, 935 s**, przyjęty przez `validate_reference_logprobs`; forward-only, więc `task07_training_authorized` pozostaje `false`. Raport: `reports/measurements/task07_reference_logprobs_2026-08-28.md`. 2026-08-28 właściciel **autoryzował trening** (`task07_training_authorized=true` w zakresie `reports/decisions/task07_training_authorization_2026-08-28.md`: trzy ramiona, kohorta 2 461 par, wybory na dev; testy zamknięte, pule v4–v11 zamknięte, bramki v2/v2.1 bez reinterpretacji). 2026-08-29 `train dpo` przestał być stubem (wspólna pętla trzech ramion, wznawialna ze stanem AdamW, walidacje przed GPU) i **wytrenowano sześć ramion**: trzy × dwa warianty par (bottom 154 kroki, near_miss 138 — zarejestrowana ablacja), raport `reports/measurements/task07_arms_dev_result_2026-08-29.md`. Na dev: DPO podnosi trafność marginesu (near_miss +18,3 pp przy trudniejszych parach) kosztem 2,2–2,4× NLL na `chosen`; continued SFT obniża NLL; weighted SFT nieodróżnialne od continued. Spot-check właściciela wykonany (95,45% zgodności, kontrola operacyjna). **Brakuje wyniku właściwego: ewaluacji probe sześciu adapterów + startu** na naturalnych zamrożonych zapytaniach; objętość danych pozostaje ograniczeniem (2,2–2,5k par wobec ablacji 20k+), a projekt trzeciej kohorty par z wadami nazwanymi z konstrukcji czeka na prospektywny ADR (`reports/plans/task07_defect_pair_pipeline_design_2026-08-29.md`). |
| [08](08_grpo_multiobjective_rl.md) | Wielokryterialny GRPO/RL | `BLOCKED` | Od 2026-08-13 planowany etap programu (cel edukacyjny właściciela, AGENTS.md §2): ścieżka 1.5B/8 GB po Task 07, R06 4.5B preferencyjnie na 16 GB; wymagana decyzja `reports/decisions/enable_grpo.md`. Porównanie DPO vs continued SFT vs best-of-N jest kontekstem interpretacyjnym; twarde przesłanki Fazy E są bramką awansu do selekcji finalistów, nie startu. Materiał przygotowawczy 2026-08-16: zmierzony w Task 06 korpus walidacyjny komponentów nagrody daje pierwsze twarde przesłanki do składu wielokryterialnej nagrody (`entity_preservation` nie karze ogólności; `format_valid` obchodzony wtrąceniem bez interpunkcji; ogólność rozdzielił `content_jaccard` w 85.6% grup). Status bez zmian — decyzja `enable_grpo.md` nadal wymagana. |
| [09](09_experiment_campaign.md) | Kampania eksperymentalna | `BLOCKED` | Model-free scaffold i fail-closed projekt pilota Task 06 są gotowe, lecz brakuje decyzji właściciela oraz rzeczywistych wyników Task 06–07, Pareto review i decyzji finalistów. Task 09 i finalne testy pozostają zamknięte. Rozszerzenie 2026-08-13: kampanię poprzedza Etap 0 — walidacja predykcyjna probe (M-01/E15); probe mieszany (M-02) i tryb produkcyjny (M-05) obowiązkowe dla finalistów; 7B+ wyłącznie na sprzęcie zewnętrznym. |
| [10](10_final_scaleup_inference_release.md) | Finalny trening, inference i release | `BLOCKED` | Wymaga wyników Task 09 i zatwierdzonego finalnego ADR. Rozszerzenie 2026-08-13: wymagana prerejestrowana, świadoma mocy reguła decyzyjna testów finalnych (M-04) oraz przenośny pipeline treningowy dla większych modeli na sprzęcie zewnętrznym. |
| [11](11_judge_robustness_audit.md) | Audyt odporności sędziego i fallbacki | `OPTIONAL` | Późny eksperyment badawczy; nie obejmuje treningu ani dostrajania rerankera. |

## Operacyjna kolejność po audycie

Ten rejestr jest jedynym operacyjnym źródłem kolejności i statusów.
[`docs/plan_poprawek_po_audytach.md`](../docs/plan_poprawek_po_audytach.md)
pozostaje zapisem przesłanek i identyfikatorów P-xx, ale nie jest równoległym
backlogiem. Zakres P-xx został przeniesiony do wskazanych plików zadań.

1. **Bramka P-05 dev screen rozstrzygnięta:** oba warianty są
   `non_inferior_only`; `dev_confirm_authorized_arms=[]`. Nie uruchamiać
   `dev_confirm` dla gold/mixed/W05 synthetic z tej macierzy.
2. **Następna kolejka baseline:** S00 i S07 są kompletne na frozen dev. S07
   pozostaje diagnostyką: jego probe użył `2485 par / 864000 tokenów / batch
   6`, a P-05 `2486 / 1152000 / batch 8`, dlatego nie wolno porównywać efektów
   ani wybierać architektury. Nie wykonywać matched-budget rerunu S07 i nie
   promować go do `dev_confirm` lub testów finalnych. P-06 mass rescoring jest
   `SUPERSEDED`: source scores pochodzą z silniejszego rerankera użytego przed
   kopaniem negatywów, próg `23.50` jest już wyegzekwowany, a minimalny margin
   frozen train wynosi `6.0`. Nie kończyć
   `artifacts/task03/p06/train_margins_v1` i nie używać go do drop/weighted.
   P06-T zmaterializował prospektywną próbkę 300 train, ale właściciel
   świadomie anulował ręczne kodowanie i zaakceptował resztkowe ryzyko
   tłumaczeń, wskazując wcześniejszy udany trening embeddera jako praktyczną
   przesłankę. Nie jest to nowy pomiar repozytorium. Frozen train pozostaje
   bez zmian, a P06-T nie jest już bramką.
   Nie zmieniać progu P-04 po obejrzeniu wyników.
   Testy finalne wolno otworzyć raz dopiero po zamrożeniu rzeczywistych
   finalistów. W06 pozostaje eksploracyjnym dowodem wykonalności 4.5B/8 GB,
   a nie ogólną zgodą na kampanię skali; jedynym wyjątkiem jest ograniczony,
   prospektywny pilot interakcji D01b 4.5B opisany w punkcie 4.
3. **Pełna bramka HN ukończona:** na wspólnej kohorcie 775/1000 query HN1
   nie odróżniło się od HN0+filter według primary, HN2 było łatwiejsze także
   według shadow, a HN3 miało konstrukcyjnie perfect primary przy przeciwnym
   kierunku shadow i `9.81%` disagreement. Utrzymano HN0+filter/drop; testów
   finalnych nie otwarto.
4. **Task 05:** D01 style/intent-only 1.5B/50k i 4.5B/50k wraz z matched
   W05/W06, common exact-K, primary/shadow/corpus scoringiem oraz copy/semantic
   `compare` są ukończone. Pełne ramiona kontrolowane zatrzymały guardraile.
   Retrospektywny D01b safe-anchor wskazał wykonalną hybrydę i niezależną
   poprawę shadow Recall@1, ale nie może autoryzować probe na tym samym dev.
   Prospective v3 uruchomiło niezmieniony selektor na niewidzianej kohorcie
   2000 rekordów: wszystkie prerejestrowane bramki przeszły, a decyzja
   `authorize_equal_budget_probe_inputs` doprowadziła do równobudżetowego
   `dev_screen` 1.5B hybrid-vs-W05. Run zakończył się `rc=0`; hybrid uzyskał
   status `eligible`, z dolną granicą 95% CI dla `corpus_ndcg_at_10`
   `0.012093457847827558` przy wymaganym minimum `+0.01`, a wszystkie
   prerejestrowane guardraile non-inferiority przeszły. Pełnobudżetowy
   `dev_confirm` na seedach 42/43/44 zakończył się następnie `rc=0`, lecz
   decyzją `non_inferior_only`: dolna granica CI `0.006927431152133765` nie
   osiągnęła wymaganego `+0.01`. Hybryda nie jest zachowana jako finalista.
   Ten zakończony eksperyment nie otworzył 4.5B ani finalnych testów.
   Późniejsza osobna decyzja właściciela dopuściła wyłącznie jednoseedowy,
   development-only pilot interakcji ze skalą: W06 4.5B vs D01b 4.5B na
   nowych rozłącznych kohortach 1000/2000. Pilot zakończył się `rc=0` i
   screeningowym `eligible`: różnica `corpus_ndcg_at_10` to
   `+0.02073792007878962`, a 95% CI
   `[0.011055484860771694, 0.03017264616376007]`; wszystkie guardraile
   przeszły. Pełny ID-only audyt znalazł tylko 591 legalnych nieoglądanych
   rekordów. Planowany 97.5% CI nie ma wystarczającej czułości wobec
   niezmiennego progu `+0.01`, także przy optymistycznym założeniu
   niezależności trzech seedów. Właściciel dostarczył następnie nowy,
   nieoglądany TriviaQA dev. Prospektywnie zamrożono 8000 query i corpus 139782
   dokumentów; leakage względem treningu wynosi zero. Trzyseedowy confirm
   zakończył się `rc=0`. Hybrid-minus-W06 `corpus_ndcg_at_10` wynosi
   `+0.04786661287844578`, a 97.5% CI
   `[0.045011840373656756, 0.05082630534799233]`; wszystkie guardraile
   przeszły. W06 seed 43 nie zbiegł i jest jawnym caveatem stabilności, ale
   symetryczna post-hoc analiza seedów 42+44 nadal daje `+0.0206102`, CI
   `[+0.0174116,+0.0237756]`. Hybrid jest zachowany do finalist-freeze review.
   Właściciel zaakceptował następnie W06+D01+selector jako procedurę danych i
   D01 jako przyszły start Task 07; model-free handoff preflight przeszedł.
   Execution ADR, pełna kampania 4.5B i finalne testy pozostają zamknięte.
   Pozostałe D00–D12 nadal wymagają własnych pomiarów i bramek.
5. **Po Task 05:** P-08 w Task 06 i Task 07. Bramka różnorodności same-prompt
   jest zamknięta metodologicznie: progi zamrożono ADR
   `reports/decisions/task06_same_prompt_diversity_gate_v1.md` przed odczytem
   par, a jej pierwsze zastosowanie na kohorcie `same_prompt_expansion_v1` dało
   362/500 grup `eligible`. Nie zmieniać progów po zobaczeniu tego wyniku i nie
   powtarzać ani nie nadpisywać zakończonego runu expansion. Deficyt do 500 par
   rozwojowej bramki dual-LLM wolno uzupełnić wyłącznie nową, osobno
   autoryzowaną generacją same-prompt (większe K z deduplikacją albo nowe grupy
   z legalnej puli), nigdy poluzowaniem bramki. Wybraną ścieżką jest ADR
   `reports/decisions/task06_same_prompt_expansion_v2_2026-08-13.md`: nowa,
   rozłączna klastrowo kohorta 500 pasaży z szerszym decodingiem. Jej kohorta
   jest już zamrożona na CPU; pozostaje etap GPU i bramka o niezmienionych
   progach. Budowa par nadal wymaga osobnego ADR zamrażającego politykę
   `chosen/rejected` i kalibrację komponentów.
6. **Kampania:** Task 09 dopiero po wcześniejszych etapach; Task 10 dopiero po
   finalnym ADR.
7. **Opcjonalne:** P-09 i Task 11 wyłącznie po własnych bramkach. Task 08
   jest od 2026-08-13 planowanym etapem po Task 07 (cel edukacyjny; bramka
   awansu do selekcji finalistów opisana w jego pliku i w AGENTS.md Faza E).

Równobudżetowy D01b probe `dev_screen` 1.5B hybrid-vs-W05 jest ukończony.
Końcowy `run-all` zakończył się `rc=0`, zapisał `dev_screen_complete`,
`dev_confirm_authorized=true` i `final_tests_used=[]`. Hybrid uzyskał
`eligible`; dolna granica 95% CI dla `corpus_ndcg_at_10` wynosi
`0.012093457847827558` przy progu `+0.01`, a wszystkie guardraile przeszły.
Nie uruchamiać ponownie dev-screen.

Pełny D01b `dev_confirm` jest ukończony dla 7936 par / 1984 dokumentów / K=4,
batch 2, 4000 kroków i seedów 42/43/44. Końcowy `run-all` zakończył się
`rc=0`; decyzja `non_inferior_only` zatrzymała promocję, ponieważ dolna granica
95% CI głównej metryki wyniosła `0.006927431152133765` przy progu `+0.01`.
Nie uruchamiać ponownie 1.5B dev-screen ani dev-confirm. Spośród nowych runów
4.5B dopuszczony jest tylko prospektywny scale-interaction pilot wskazany w
tabeli; pełna kampania 4.5B i wszystkie finalne testy pozostają zamknięte.
Nie uruchamiać pełnego
`scripts/score_train_margins.py`, nie ustalać lokalnego progu i nie trenować
ordinary/drop/weighted bez nowego prospektywnego ADR opartego na ręcznie
potwierdzonej, powtarzalnej klasie błędu tłumaczenia.
Nie należy ponownie uruchamiać
`scripts/run_p05_dev_screen.sh` ani `scripts/run_p05_guardrails.sh`: wyniki i
decyzje są kompletne. I02/I04/I05 pozostają odroczone, S00 jest zmierzone,
S07 diagnostycznie kompletne, P-06 mass rescoring zamknięte jako
`SUPERSEDED`, ręczne P06-T świadomie anulowano, a
niewykonane D00–D12, Task 06 i wszystkie testy finalne pozostają zamknięte.
D01 `dev_confirm` został zakończony wyłącznie na train/dev bez użycia final test.

## Kolejność bazowa

Po domknięciu pakietu naprawczego kolejność bazowa to
`00 → 01 → 02 → 03 → 04 → 05 → 06 → 07 → 08 → 09 → 10`.
Task 04 może częściowo powstawać równolegle z Task 03. Task 08 jest od
2026-08-13 planowanym etapem po Task 07 (cel edukacyjny właściciela; bramka
awansu wyników do selekcji finalistów w jego pliku i w AGENTS.md Faza E).
Task 11 pozostaje opcjonalny i wolno go rozpocząć wyłącznie po spełnieniu
warunków opisanych w jego pliku. Nadrzędne bramki badawcze i zasady
bezpieczeństwa znajdują się w [`AGENTS.md`](../AGENTS.md).

Aktualizacja specyfikacji 2026-08-13 (decyzja właściciela): kontrakt
sprzętowy w `AGENTS.md` §12 opisuje odtąd 8 GB (RTX 3060 Ti) jako budżet
bazowy, 16 GB (RTX 5070 Ti, 64 GB RAM) jako rozszerzenie oraz sprzęt
zewnętrzny dla finalistów 7B+ przez przenośny pipeline. Rozszerzenia
metodologiczne M-01–M-05 (walidacja predykcyjna probe, probe mieszany,
stabilność seedów, reguła decyzyjna testów finalnych, tryb produkcyjny) są
zdefiniowane w `AGENTS.md` §9.2 i rozpisane w Taskach 04, 06, 07, 09 i 10.
GRPO (Task 08) jest planowanym etapem programu z jawnym celem edukacyjnym
(AGENTS.md §2) i osobną bramką awansu wyników do selekcji finalistów. Zmiany
są prospektywne i nie unieważniają żadnych zamrożonych kontraktów, kohort
ani zakończonych pomiarów.
